# LaTeX view lens (descriptor)

The document view imports a LaTeX article through the shared markup codec and opens the same
document value through the formatted article lens and the source lens. This recipe records the
surface workflow; the cookbook descriptor names the lenses rather than launching a browser.
