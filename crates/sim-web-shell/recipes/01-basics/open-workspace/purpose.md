# Open Web Workspace (descriptor)

Documents the browser shell startup path and the first live session descriptor.
The WebUI is served by the shell and rendered in a browser; this recipe records
the launch shape rather than reducing it through the cookbook sandbox.
